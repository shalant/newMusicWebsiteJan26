<section class="light" id="education">
    <h2>Education</h2>
    <div style="width: 33%;align-items:center;">
        @if (musicalSheets is null)
        {
            <p><em>Loading...</em></p>
        }
        else
        {
            <select @onchange="OnChange">
                <option value="">Select an option</option>
                @foreach (var option in musicalSheets)
                {
                    <option value="@option.Id">@option.Title</option>
                }
            </select>
        
            <p>Selected ID: @selectedId</p>
        }
    </div>
</section>

@code {
    [Inject]
    NavigationManager NavigationManager { get; set; } = default!;

    [Parameter, EditorRequired]
    public required HttpClient Http { get; set; }

    [Parameter, EditorRequired]
    public required HeroImageService HeroImageService { get; set; }

    private int SongValue { get; set; } = 0;

    private List<SheetMusic>? musicalSheets;
    private HeroImage? hero;

    public string SelectedSheetMusicString { get; set; } = "test";
    public SheetMusic? SelectedSheetMusic { get; set; }
    public int? SelectedSheetMusicId { get; set; }
    public List<SheetMusic> SheetMusics { get; set; } = new List<SheetMusic>();

    private int selectedId;
    private void OnChange(ChangeEventArgs e)
    {
        if (musicalSheets is not null)
        {
            // Get the selected option ID
            selectedId = int.Parse(e.Value.ToString());
            Console.WriteLine($"Selected ID: {selectedId}");
            var hyperlink = musicalSheets.FirstOrDefault(f => f.Id == selectedId)?.URL;
            NavigationManager.NavigateTo($"/sheetmusic/{hyperlink}");
        }
    }

    protected override async Task OnInitializedAsync()
    {
        musicalSheets = await Http.GetFromJsonAsync<List<SheetMusic>>("sample-data/sheetmusic.json");
        hero = await HeroImageService.GetHeroAsync(img => img.Name is "portfolio");

        SheetMusics = new List<SheetMusic>
        {
            new SheetMusic { Id = 1, Title = "Fur Elise", URL = "Ludwig van Beethoven" },
            new SheetMusic { Id = 2, Title = "Moonlight Sonata", URL = "Ludwig van Beethoven" },
            new SheetMusic { Id = 3, Title = "Clair de Lune", URL = "Claude Debussy" }
        };
    }

    
}




@* rando mudselect code
        @* <MudSelect @bind-Value="SelectedSheetMusicString" Label="Select sheet music" T="string" Placeholder="Please Select" AdornmentIcon="@Icons.Material.Filled.LibraryMusic" AdornmentColor="Color.Primary">
        <MudSelect @bind-Value="SelectedSheetMusicString" T="string" Variant="Variant.Text" ValueChanged="OpenSheetMusic">
            @if (musicalSheets is not null)
            {
                @* @foreach (var item in musicalSheets)
                {
                    <MudSelectItem Value="@item.Title" T="string">@item.Title</MudSelectItem>
                }
                @* <MudSelectItem Value="@("One")">One</MudSelectItem>
                <MudSelectItem Value="@("Two")">Two</MudSelectItem>
                <MudSelectItem Value="@("Three")">Three</MudSelectItem>
                <MudSelectItem Value="@("Four")">Four</MudSelectItem>
            }
            @foreach (var item in _states)
            {
                <MudSelectItem Value="@item" >@item</MudSelectItem>
            }
            }
        </MudSelect>


        <MudSelect @bind-Value="_value"
                       Variant="Variant.Text"
                       >
        @foreach (var state in _states)
                {
                    <MudSelectItem Value="state">@state</MudSelectItem>
                }
            </MudSelect>



            @* <select class="form-control" @onchange="@((args)=>OpenSheetMusic(args, SelectedSheetMusic))">         <select class="form-control">
            <option value="-1">Select an song!</option>
            @foreach (var item in SheetMusics)
            {
                @* <option value="@item.Id" @onclick="()=>SongValue=item.Id">@item.Title</option>
                <option value="@item.Id" @onclick="() => SongValue = item.Id">
                    @* <option value="@item.Id" > *@
@* <a href="@item.URL" @onclick="@((args) => OpenSheetMusic(args, item))">@item.Title</a>
                    @* <a href="@item.URL">@item.Title</a>
                    @item.Title
                </option>
            }
        </select>


        protected void OpenSheetMusic(MouseEventArgs e, SheetMusic selectedSong)
    {

        Console.WriteLine($"{selectedSong}");
        SelectedSheetMusic = musicalSheets.FirstOrDefault(f => f.Id == selectedSong.Id);
        NavigationManager.NavigateTo($"{selectedSong.URL}");

    }

    private readonly string[] _states =
{
        "Alabama", "Alaska", "Arizona", "Arkansas", "California",
        "Colorado", "Connecticut", "Delaware", "Florida", "Georgia",
        "Hawaii", "Idaho", "Illinois", "Indiana", "Iowa", "Kansas",
        "Kentucky", "Louisiana", "Maine", "Maryland", "Massachusetts",
        "Michigan", "Minnesota", "Mississippi", "Missouri", "Montana",
        "Nebraska", "Nevada", "New Hampshire", "New Jersey", "New Mexico",
        "New York", "North Carolina", "North Dakota", "Ohio", "Oklahoma",
        "Oregon", "Pennsylvania", "Rhode Island", "South Carolina", "South Dakota",
        "Tennessee", "Texas", "Utah", "Vermont", "Virginia",
        "Washington", "West Virginia", "Wisconsin", "Wyoming"
    };
            *@